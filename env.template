# =============================================================================
# jarvis-mcp - Environment Template
# =============================================================================
# Copy to .env and fill in values
# Port: 8011
# MCP server for Claude Code integration
# =============================================================================

# Service URLs are resolved automatically from jarvis-config-service

# -----------------------------------------------------------------------------
# SERVER
# -----------------------------------------------------------------------------
JARVIS_MCP_HOST=localhost
JARVIS_MCP_PORT=8011

# Tool groups to enable (comma-separated): logs, debug
JARVIS_MCP_TOOLS=logs,debug

# -----------------------------------------------------------------------------
# SERVICE DISCOVERY
# -----------------------------------------------------------------------------
# Config service URL (preferred - fetches all service URLs automatically)
JARVIS_CONFIG_URL=http://localhost:8013

# Note: Only set JARVIS_CONFIG_URL_STYLE=dockerized when running in Docker
# JARVIS_CONFIG_URL_STYLE=dockerized

# -----------------------------------------------------------------------------
# AUTHENTICATION
# -----------------------------------------------------------------------------
# App credentials for authenticated endpoints
JARVIS_APP_ID=jarvis-mcp
JARVIS_APP_KEY=CHANGE_ME_get_from_jarvis_auth_admin
